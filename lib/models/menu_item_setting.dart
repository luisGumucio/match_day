import 'package:flutter/material.dart';

class MenuItemSetting {
  final IconData icon;
  final String title;
  final String subtitle;
  final Color color;
  final Widget page;

  MenuItemSetting(this.icon, this.title, this.subtitle, this.color, this.page);
}